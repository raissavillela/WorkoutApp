kind = "web"
previewPath = "/"
title = "Workout App"
version = "1.0.0"
id = "artifacts/workout"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21604

[services.development]
run = "pnpm --filter @workspace/workout run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/workout", "run", "build" ]
publicDir = "artifacts/workout/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21604"
BASE_PATH = "/"
